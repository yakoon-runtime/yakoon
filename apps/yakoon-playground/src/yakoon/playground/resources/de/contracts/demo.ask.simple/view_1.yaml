kind: projection

projection:
  role: info
  title: "Commands"
  blocks:

    - type: fields
      input_mode: prompt
      fields:
        - { var: result, policy: system:string, title: "Username" } 

