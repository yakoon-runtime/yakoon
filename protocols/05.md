*(Aktuelles Thema steht immer oben)*
# Protkoll May



---
### Thema: version
Wie wollen wir das mit der Platform-Versoin machen? Wir würden gerne eine Version einführen, um zu sehen, welche Version gerade aktiv läuft.
- Über die Version: Major, Minor .... Was ist sinnvoll? Macht es vielleicht sogar Sinn, die Version an Git zu koppeln. Macht es Sinn, Tags für die Version einzuführen? Also jede Version in Git zu taggen und dann diese Version im Command anzuzeigen?
- Was ist wenn dir die Version später über einen Docker ausrollen?
- Wollen wir den Docker Lese-Zugriff aufs Git Repo geben? Dann könnte diese per Remote-Command sogar Daten aus dem Repo ziehen und somit ein Restart der Platform durchführen. Der Docker-Container könnte auch von außen ein "docker yakoon - update" erhalten.
- Brauchen wir dann Settings.platform_version noch? Oder versuchen wir über OS.get.... die Version aus dem File zu lesen. Dieses könnten wir ja handisch erstmal führen. Später übernimmt das der Docker-Build

**Lösung**
- ausschließlich annotated Tags verwenden
- git tag -a v0.3.1 -m "Yakoon version 0.3.1"

---
### Thema: Injection commandsets
Allgemeine Commands, welche in jeder Domain sind (help, switch...) sollten per injection ohne explizite Registierung innerhalb der Domains verfügbar sein.
- Lösung: Wir definieren ein gemeinsames commandset innerhalb der Platform mit entsprechenden Commands
  aus der Platform. Nennen wir das commandset mal "share". Dieses Commandset registrieren wir dann einfach
  in den einzelnen Domains (1 Zeile code + 1 import)
- Wir brauchen vernünftige Kategorien: Login / Account, system, admin, help (vielleicht finden wir bessere.)
  Würden wir erwarten, dass eine Kategory system mit oder ohne Anmeldung funktioniert? Beinhaltet system
  funktionen wie help, oder eher funktionen wir create_user

---
### Thema: loging an Plattform - und/nicht an Domain
Nachdem wir uns an der Plattform angemeldet haben, ist es manchmal notwendig, sich erneut anzumelden. Manchmal 
aber auch nicht. Das kann jede Domain selbst entscheiden. Dazu kann die Domain einen entsprechenden Text ausgeben, wenn das notwendig ist. Der Ort dafür könnte das Command switch sein. Switch gibt dann eine Willkommensmeldung aus und informiert, wie die Anmeldung erfolgen soll. Das Command Switch schaltet die domain dann in der Session auf die aktuelle domain. Dazu braucht es noch keinen Account.

**Problem**
- session.command_groups = account.groups wird durch den aktuellen Account überschrieben. command_groups
  wird an den verschiedensten Stellen geprüft. Diese werden bei jeder Session beim initialisieren durch
  die domains festgelegt. Somit legt jede Domain fest, was eine Session per Default tun darf 
  (Festlegung der commandsets.) Durch die Anmeldung (Account) kommen nun aber weitere groups hinzu. 
  Diese müssen der command_groups hinzugefügt werden, um nicht an allen Stellen im Code immer prüfen zu müssen:
  session.commandgroups + account. 

  Überlegung: Nachdem jede Session bei jedem Request neu aufgebaut, und der Account geladen wird (wenn account_id gesetzt), können wir die 
  account.cmd_groups direkt in die session übergeben und müssen diese nicht seperat an der Session verwalten.

  - Bedeutet: Nur der Account speichert die cmd_groups später im Storage. Die Session bekommt ihre gruppen druch die Domains + den Account 
    batch: login stefan; switch mud; help

----
### Thema: command_groups
command_groups beinhaltet die Bezeichnung aller gültigen commandsets. Diese werden von den Domains bereitgestellt und in die Session initial übertragen. 

**Probleme**
- Namen könnten doppelt sein -> Lösung: Prefix in Namen angeben!
