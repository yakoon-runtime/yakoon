version: 1.0.0

name: set
title: Change session settings

resolvable: true
navigable: false
contextual: false

expose: false

executor: runtime

entry:
  run: app.py

invocation:
  min_options: 1
  params:
    - key: interaction

man:
  default: resources/man.ydf
