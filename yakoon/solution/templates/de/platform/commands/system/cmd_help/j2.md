{%- if section.key == "show" %}
# Hilfe

## 🛰️ Verfügbare Domains:
{% for controller in section.data.controllers %}
  - [switch](# "switch {{controller.id}}") → {{ controller.id|ljust(10) }}  
{% endfor %}

## 🧩 Globale Befehle:
{% for category, commands in section.data.grouped.items() %}
### {{category|title}}
> {% for cmd in commands %}[{{cmd.key}}](# "{{cmd.key}}")  {% endfor %}

{% endfor %}
{%- else %}
Unbekannter Schlüssel: {{ section.key }}

{%- endif %}