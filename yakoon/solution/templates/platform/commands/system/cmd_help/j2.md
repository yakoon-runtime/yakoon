# Hilfe

## 🛰️ Verfügbare Domains:
{% for controller in controllers %}
  - [switch](# "switch {{controller.name}}") → {{ controller.name|ljust(10) }}  
{% endfor %}

## 🧩 Globale Befehle:
{% for category, commands in grouped.items() %}
### {{category|title}}
> {% for cmd in commands %}[{{cmd.key}}](# "{{cmd.key}}")  {% endfor %}

{% endfor %}

