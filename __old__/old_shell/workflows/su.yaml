workflow: auth_user
version: 1

start: to_auth

steps:
  - id: to_auth
    run: "use auth"
    next: auth:su

  - id: auth:su
    run: "su --user {{user.name}} --password {{user.password}}"
    next: leave

  - id: leave
    run: "exit"
    next: done
  
  - id: done
    end: ok