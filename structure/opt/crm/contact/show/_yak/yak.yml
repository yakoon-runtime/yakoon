title: Contact Show

host: /boot/python/runtime

entry:
  run: pack:y5n.apps.crm.apps.contact.show:main

invocation:
  params:
    - key: name
      positional: true
      required: true

document:
  default: file:resources/default.ydf

man:
  default: file:resources/man.ydf
