title: Contact Delete

host: /boot/python/runtime

entry:
  run: pack:y5n.apps.crm.apps.contact.delete:main

invocation:
  params:
    - key: name
      positional: true
      required: true

document:
  default: file:resources/default.ydf

man:
  default: file:resources/man.ydf
