title: User Edit

host: /boot/python/runtime

entry:
  run: pack:y5n.apps.ident.apps.users.edit:main

invocation:
  params:
    - key: name
      positional: true
      required: true
    - key: password
    - key: enabled

document:
  default: file:resources/default.ydf

man:
  default: file:resources/man.ydf
