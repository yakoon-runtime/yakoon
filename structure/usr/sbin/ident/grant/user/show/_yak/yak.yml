title: Grant to User

host: /boot/python/runtime

entry:
  run: pack:y5nident.apps.grants.user_show:main

invocation:
  params:
    - key: name
      positional: true
      required: true
    - key: permission
      positional: true
      required: true

document:
  default: file:resources/default.ydf

man:
  default: file:resources/man.ydf
