title: Join Groups (for User)

resolvable: true
navigable: false
contextual: false
host: /boot/python/runtime

entry:
  run: pack:y5n.apps.ident.apps.joins.groups:main

invocation:
  params:
    - key: user
      positional: true
      required: true

document:
  default: file:resources/default.ydf

man:
  default: file:resources/man.ydf
