title: Join Remove

host: /boot/python/runtime

entry:
  run: pack:y5nident.apps.joins.remove:main

invocation:
  params:
    - key: user
      positional: true
      required: true
    - key: group
      positional: true
      required: true

document:
  default: file:resources/default.ydf

man:
  default: file:resources/man.ydf
