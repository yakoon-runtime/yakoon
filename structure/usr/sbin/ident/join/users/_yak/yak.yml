title: Join Users (in Group)

host: /boot/python/runtime

entry:
  run: pack:y5n.apps.ident.apps.joins.users:main

invocation:
  params:
    - key: group
      positional: true
      required: true

document:
  default: file:resources/default.ydf

man:
  default: file:resources/man.ydf
