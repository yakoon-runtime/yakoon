title: Su

host: /boot/python/runtime

entry:
  run: pack:y5nident.apps.su:main

invocation:
  params:
    - key: user
      positional: true
    - key: password

document:
  default: file:resources/default.ydf

man:
  default: file:resources/man.ydf
