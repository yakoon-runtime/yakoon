kind: projection

projection:
  role: info
  title: "Commands"
  blocks:
    - type: text
      text: "Geben Sie ein Passwort ein. (Eingabe nicht sichtbar)"

    - type: fields
      fields:
        - { name: auth.password, policy: system:masked, title: "Password"} 