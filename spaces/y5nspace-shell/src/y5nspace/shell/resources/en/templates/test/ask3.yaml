kind: projection

projection:
  role: info
  title: "Commands"
  blocks:
    - type: text
      text: "Noch eine Bemerkung..."

    - type: fields
      fields:
        - { key: the_key, name: result, policy: system:string, title: "Bemerkung" } 
