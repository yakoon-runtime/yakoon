[project]
name = "yak"
version = "0.1.0"
description = "Yakoon Platform Manager"
requires-python = ">=3.13"

[project.scripts]
yak = "yak.hosts.cli.main:main"

[tool.setuptools.packages.find]
where = ["src"]
