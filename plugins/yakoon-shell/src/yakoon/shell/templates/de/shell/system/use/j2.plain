{%- if section.key == "show" %}
{%- for c in section.data.controllers %}
  {{c.id}} 
{%- endfor -%}

{%- elif section.key == "activated" %}

{%- elif section.key == "already_in_shell" %}
Programm "{{section.data.controller.id}}" ist bereits aktiv.

{%- elif section.key == "name_not_found" %}
Das Programm "{{section.data.name}}" wurde nicht gefunden.

{%- endif %}
