kind: projection

projection:
  role: info
  title: "Commands"
  blocks:
    - type: text
      text: "Suche nach Projekten:"

    - type: fields
      fields:
        - { var: result, policy: system:string, title: "Projektname", lookup: city.show.all } 

    - type: actions
      actions:
        - label: Speichern
          command: project.save